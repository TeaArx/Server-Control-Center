<Window x:Class="ServerControlCenter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:ServerControlCenter.Controls"
        xmlns:services="clr-namespace:ServerControlCenter.Services"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Server Control Center"
        Width="1360"
        Height="900"
        MinWidth="1180"
        MinHeight="760"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="PackIconStyle"
               TargetType="{x:Type iconPacks:PackIconMaterial}">
            <Setter Property="Width"
                    Value="17"/>
            <Setter Property="Height"
                    Value="17"/>
            <Setter Property="Foreground"
                    Value="{StaticResource TextBrush}"/>
            <Setter Property="VerticalAlignment"
                    Value="Center"/>
            <Setter Property="HorizontalAlignment"
                    Value="Center"/>
            <Setter Property="SnapsToDevicePixels"
                    Value="True"/>
        </Style>
        <Style x:Key="ModernTabControlStyle"
               TargetType="{x:Type TabControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    Background="#07111B"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="0 0 0 1"
                                    Padding="16 8 16 7">
                                <TabPanel IsItemsHost="True"
                                          Background="Transparent"
                                          Margin="0"/>
                            </Border>

                            <ContentPresenter Grid.Row="1"
                                              ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTabItemStyle"
               TargetType="{x:Type TabItem}">
            <Setter Property="Foreground"
                    Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontWeight"
                    Value="SemiBold"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="Padding"
                    Value="14 8"/>
            <Setter Property="Margin"
                    Value="0 0 10 0"/>
            <Setter Property="MinHeight"
                    Value="36"/>
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderBrush"
                    Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="TabBorder"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Foreground"
                                        Value="{StaticResource TextBrush}"/>
                                <Setter TargetName="TabBorder"
                                        Property="Background"
                                        Value="#0D1A27"/>
                                <Setter TargetName="TabBorder"
                                        Property="BorderBrush"
                                        Value="#1D3348"/>
                            </Trigger>

                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Foreground"
                                        Value="{StaticResource TextBrush}"/>
                                <Setter TargetName="TabBorder"
                                        Property="Background"
                                        Value="#174A7C"/>
                                <Setter TargetName="TabBorder"
                                        Property="BorderBrush"
                                        Value="#2D6FA8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="248"/>
            <ColumnDefinition Width="*"
                              MinWidth="880"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="62"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="2"
                Grid.Column="0"
                Background="{StaticResource ShellPanelBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="0 0 1 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="62"/>
                    <RowDefinition Height="116"/>
                    <RowDefinition Height="178"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="64"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Padding="18 0"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="0 0 0 1">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Border Width="26"
                                Height="26"
                                CornerRadius="6"
                                Background="{StaticResource AccentBrush}"
                                Margin="0 0 10 0">
                            <Grid>
                                <iconPacks:PackIconMaterial Kind="Server"
                                                            Width="15"
                                                            Height="15"
                                                            Foreground="White"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                <Border Width="12"
                                        Height="12"
                                        CornerRadius="3"
                                        BorderBrush="White"
                                        BorderThickness="1.2"
                                        Opacity="0.78"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <TextBlock Text="ServerControl"
                                   FontSize="16"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1"
                            Margin="14 14 14 12">
                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding AddServerCommand}"
                            Height="38"
                            ToolTip="{Binding [AddServer], Source={x:Static services:AppServices.Localizer}}"
                            Margin="0 0 0 14">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Plus"
                                                        Style="{StaticResource PackIconStyle}"
                                                        Margin="0 0 8 0"/>
                            <TextBlock Text="{Binding [AddServer], Source={x:Static services:AppServices.Localizer}}"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <TextBlock Text="{Binding [SearchServers], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"
                               Margin="4 0 0 0"/>
                </StackPanel>

                <StackPanel Grid.Row="2"
                            Margin="14 0 14 10">
                    <Grid Margin="4 0 2 8">
                        <TextBlock Text="{Binding [Groups], Source={x:Static services:AppServices.Localizer}}"
                                   Style="{StaticResource SmallMutedTextStyle}"/>

                    </Grid>
                    <ListBox ItemsSource="{Binding Groups}"
                             MaxHeight="142">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.SelectGroupCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding Name}"
                                        HorizontalContentAlignment="Stretch"
                                        ToolTip="{Binding Name}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="0">
                                    <Border Padding="8 7"
                                            CornerRadius="6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background"
                                                        Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}"
                                                                 Value="True">
                                                        <Setter Property="Background"
                                                                Value="{StaticResource SubtleBackgroundBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="22"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <iconPacks:PackIconMaterial Grid.Column="0"
                                                                        Kind="FolderMultiple"
                                                                        Style="{StaticResource PackIconStyle}"
                                                                        Foreground="{StaticResource MutedTextBrush}"/>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding Count}"
                                                       Foreground="{StaticResource AccentBrush}"
                                                       Margin="8 0 0 0"/>
                                        </Grid>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <StackPanel Grid.Row="3"
                            Margin="14 0">
                    <TextBlock Text="{Binding [Servers], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"
                               Margin="4 0 0 10"/>
                    <ListBox ItemsSource="{Binding FilteredServers}"
                             SelectedItem="{Binding SelectedServer}"
                             MouseDoubleClick="ServersListBox_MouseDoubleClick"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="{Binding [Edit], Source={x:Static services:AppServices.Localizer}}"
                                          Command="{Binding EditServerCommand}">
                                    <MenuItem.Icon>
                                        <iconPacks:PackIconMaterial Kind="Pencil"
                                                                    Style="{StaticResource PackIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="{Binding [TestSsh], Source={x:Static services:AppServices.Localizer}}"
                                          Command="{Binding TestConnectionCommand}">
                                    <MenuItem.Icon>
                                        <iconPacks:PackIconMaterial Kind="LanConnect"
                                                                    Style="{StaticResource PackIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="{Binding [ToggleFavorite], Source={x:Static services:AppServices.Localizer}}"
                                          Command="{Binding ToggleSelectedServerFavoriteCommand}">
                                    <MenuItem.Icon>
                                        <iconPacks:PackIconMaterial Kind="Star"
                                                                    Style="{StaticResource PackIconStyle}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="{Binding [Delete], Source={x:Static services:AppServices.Localizer}}"
                                          Command="{Binding DeleteServerCommand}">
                                    <MenuItem.Icon>
                                        <iconPacks:PackIconMaterial Kind="Delete"
                                                                    Style="{StaticResource PackIconStyle}"
                                                                    Foreground="{StaticResource DangerBrush}"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </ListBox.ContextMenu>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10"
                                        CornerRadius="6"
                                        Background="{StaticResource CardPanelBrush}"
                                        BorderBrush="{StaticResource BorderBrushSoft}"
                                        BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Grid.Column="0"
                                                 Width="9"
                                                 Height="9"
                                                 Margin="0 5 10 0"
                                                 VerticalAlignment="Top">
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Setter Property="Fill"
                                                            Value="#8CA0B2"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsOnline}"
                                                                     Value="True">
                                                            <Setter Property="Fill"
                                                                    Value="{StaticResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding DisplayOs}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       Margin="0 2 0 0"/>
                                            <TextBlock Text="{Binding DisplayAddress}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                        <iconPacks:PackIconMaterial Grid.Column="2"
                                                                    Kind="Star"
                                                                    Width="16"
                                                                    Height="16"
                                                                    Margin="10 0 0 0">
                                            <iconPacks:PackIconMaterial.Style>
                                                <Style TargetType="iconPacks:PackIconMaterial"
                                                       BasedOn="{StaticResource PackIconStyle}">
                                                    <Setter Property="Foreground"
                                                            Value="{StaticResource MutedTextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFavorite}"
                                                                     Value="True">
                                                            <Setter Property="Foreground"
                                                                    Value="{StaticResource WarningBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </iconPacks:PackIconMaterial.Style>
                                        </iconPacks:PackIconMaterial>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <Border Grid.Row="4"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="0 1 0 0"
                        Padding="14 10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0"
                                BorderThickness="0"
                                Background="Transparent"
                                HorizontalAlignment="Left"
                                Command="{Binding AddServerCommand}"
                                ToolTip="{Binding [AddServer], Source={x:Static services:AppServices.Localizer}}">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Plus"
                                                            Style="{StaticResource PackIconStyle}"
                                                            Margin="0 0 0 0"/>
                            </StackPanel>
                        </Button>
                        <Button Grid.Column="1"
                                Style="{StaticResource IconButtonStyle}"
                                ToolTip="{Binding [Favorites], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding OpenFavoritesPanelCommand}"
                                Margin="4 0">
                            <iconPacks:PackIconMaterial Kind="Star"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                        <Button Grid.Column="2"
                                Style="{StaticResource IconButtonStyle}"
                                ToolTip="{Binding [Settings], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding OpenSettingsPanelCommand}"
                                Margin="4 0">
                            <iconPacks:PackIconMaterial Kind="Cog"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                        <Button Grid.Column="3"
                                Style="{StaticResource IconButtonStyle}"
                                ToolTip="{Binding [Activity], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding OpenActivityPanelCommand}"
                                Margin="4 0 0 0">
                            <iconPacks:PackIconMaterial Kind="Bell"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Grid.Row="0"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="0 0 0 1"
                Padding="18 0"
                Background="#070E15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0 0 18 0">
                    <Ellipse Width="7"
                             Height="7"
                             Margin="0 0 14 0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill"
                                        Value="#8CA0B2"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedServer.IsOnline}"
                                                 Value="True">
                                        <Setter Property="Fill"
                                                Value="{StaticResource SuccessBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding SelectedServerTitle}"
                               FontSize="16"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="&#x2022;"
                               Foreground="{StaticResource MutedTextBrush}"
                               Margin="18 0 14 0"/>
                    <Ellipse Width="7"
                             Height="7"
                             Margin="0 0 8 0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill"
                                        Value="#8CA0B2"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedServer.IsOnline}"
                                                 Value="True">
                                        <Setter Property="Fill"
                                                Value="{StaticResource SuccessBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding OnlineText}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock"
                                   BasedOn="{StaticResource {x:Type TextBlock}}">
                                <Setter Property="Foreground"
                                        Value="{StaticResource MutedTextBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedServer.IsOnline}"
                                                 Value="True">
                                        <Setter Property="Foreground"
                                                Value="{StaticResource SuccessBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                <Grid Grid.Column="1"
                      Width="230"
                      VerticalAlignment="Center"
                      Margin="0 0 16 0">
                    <TextBox x:Name="ServerSearchBox"
                             Text="{Binding ServerSearchText, UpdateSourceTrigger=PropertyChanged}"
                             Padding="34 5 78 5"
                             ToolTip="{Binding [SearchServers], Source={x:Static services:AppServices.Localizer}}"/>
                    <iconPacks:PackIconMaterial Kind="Magnify"
                                                Width="16"
                                                Height="16"
                                                Foreground="{StaticResource MutedTextBrush}"
                                                Margin="12 0 0 0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"/>
                    <Border Background="#101B28"
                            BorderBrush="{StaticResource BorderBrushSoft}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="5 1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0 0 6 0">
                        <TextBlock Text="Ctrl K / F"
                                   Style="{StaticResource TinyMutedTextStyle}"/>
                    </Border>
                </Grid>
                <Button Grid.Column="2"
                        Style="{StaticResource IconButtonStyle}"
                        ToolTip="{Binding [CheckAllServers], Source={x:Static services:AppServices.Localizer}}"
                        Command="{Binding CheckAllServersCommand}"
                        Margin="0 0 8 0">
                    <iconPacks:PackIconMaterial Kind="Refresh"
                                                Style="{StaticResource PackIconStyle}"/>
                </Button>
                <StackPanel Grid.Column="3"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButtonStyle}"
                            ToolTip="{Binding [Activity], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding OpenActivityPanelCommand}"
                            Margin="0 0 8 0">
                        <iconPacks:PackIconMaterial Kind="Bell"
                                                    Style="{StaticResource PackIconStyle}"/>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}"
                            ToolTip="{Binding [Settings], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding OpenSettingsPanelCommand}"
                            Margin="0 0 8 0">
                        <iconPacks:PackIconMaterial Kind="Cog"
                                                    Style="{StaticResource PackIconStyle}"/>
                    </Button>
                    <Button Command="{Binding OpenProfilePanelCommand}"
                            Style="{StaticResource AvatarButtonStyle}"
                            ToolTip="Profile">
                        <Grid Width="40"
                              Height="40">
                            <Border Width="32"
                                    Height="32"
                                    CornerRadius="16"
                                    Background="{StaticResource AccentBrush}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding UserProfile.Initials}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontWeight="Bold"/>
                            </Border>
                            <Ellipse Width="9"
                                     Height="9"
                                     Fill="{StaticResource SuccessBrush}"
                                     Stroke="{StaticResource AppBackgroundBrush}"
                                     StrokeThickness="2"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Bottom"
                                     Margin="0 0 4 4"/>
                        </Grid>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1"
              Grid.Row="1"
              Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="124"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="0.9*"
                               MinHeight="220"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="1.25*"
                               MinHeight="300"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"
                                      MinWidth="222"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"
                                      MinWidth="222"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"
                                      MinWidth="222"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="160"
                                      MinWidth="150"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Style="{StaticResource MetricCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="118"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource IconBadgeStyle}"
                                    Margin="0 0 10 0">
                                <iconPacks:PackIconMaterial Kind="Chip"
                                                            Width="17"
                                                            Height="17"
                                                            Foreground="{StaticResource AccentBrush}"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="CPU"
                                       Style="{StaticResource SectionTitleTextStyle}"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1"
                                   Text="{Binding MonitorCpuPercent, StringFormat={}{0:0}%}"
                                   FontSize="30"
                                   Margin="44 6 0 0"/>
                        <TextBlock Grid.Row="2"
                                   Text="{Binding MonitorCpu}"
                                   Style="{StaticResource SmallMutedTextStyle}"
                                   TextTrimming="CharacterEllipsis"/>
                        <controls:SparklineChart Grid.Column="1"
                                                 Grid.RowSpan="3"
                                                 Values="{Binding CpuHistory}"
                                                 Stroke="{StaticResource AccentBrush}"
                                                 Fill="{StaticResource AccentChartFillBrush}"
                                                 ShowGrid="False"
                                                 Height="76"
                                                 VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <Border Grid.Column="2"
                        Style="{StaticResource MetricCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="118"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource IconBadgeStyle}"
                                    Background="#1C1441"
                                    BorderBrush="#3E2673"
                                    Margin="0 0 10 0">
                                <iconPacks:PackIconMaterial Kind="Memory"
                                                            Width="17"
                                                            Height="17"
                                                            Foreground="{StaticResource PurpleBrush}"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="RAM"
                                       Style="{StaticResource SectionTitleTextStyle}"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1"
                                   Text="{Binding MonitorRamPercent, StringFormat={}{0:0}%}"
                                   FontSize="30"
                                   Margin="44 6 0 0"/>
                        <TextBlock Grid.Row="2"
                                   Text="{Binding MonitorRam}"
                                   Style="{StaticResource SmallMutedTextStyle}"
                                   TextTrimming="CharacterEllipsis"/>
                        <controls:SparklineChart Grid.Column="1"
                                                 Grid.RowSpan="3"
                                                 Values="{Binding RamHistory}"
                                                 Stroke="{StaticResource PurpleBrush}"
                                                 Fill="{StaticResource PurpleChartFillBrush}"
                                                 ShowGrid="False"
                                                 Height="76"
                                                 VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <Border Grid.Column="4"
                        Style="{StaticResource MetricCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="118"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource IconBadgeStyle}"
                                    Background="#0C3629"
                                    BorderBrush="#1F6049"
                                    Margin="0 0 10 0">
                                <iconPacks:PackIconMaterial Kind="Harddisk"
                                                            Width="17"
                                                            Height="17"
                                                            Foreground="{StaticResource SuccessBrush}"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="DISK"
                                       Style="{StaticResource SectionTitleTextStyle}"/>
                        </StackPanel>
                        <TextBlock Grid.Row="1"
                                   Text="{Binding MonitorDiskPercent, StringFormat={}{0:0}%}"
                                   FontSize="30"
                                   Margin="44 6 0 0"/>
                        <TextBlock Grid.Row="2"
                                   Text="{Binding MonitorDiskUsage}"
                                   Style="{StaticResource SmallMutedTextStyle}"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding MonitorDiskUsage}"/>
                        <controls:SparklineChart Grid.Column="1"
                                                 Grid.RowSpan="3"
                                                 Values="{Binding DiskHistory}"
                                                 Stroke="{StaticResource SuccessBrush}"
                                                 Fill="{StaticResource SuccessChartFillBrush}"
                                                 ShowGrid="False"
                                                 Height="76"
                                                 VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <Border Grid.Column="6"
                        Style="{StaticResource MetricCardStyle}"
                        Padding="14 12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Style="{StaticResource IconBadgeStyle}"
                                    Margin="0 0 10 0">
                                <iconPacks:PackIconMaterial Kind="ClockOutline"
                                                            Width="17"
                                                            Height="17"
                                                            Foreground="{StaticResource AccentBrush}"
                                                            HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{Binding [Uptime], Source={x:Static services:AppServices.Localizer}}"
                                       Style="{StaticResource SectionTitleTextStyle}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding MonitorUptimeShort}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0 12 0 7"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding MonitorUptimeShort}"/>
                        <TextBlock Text="{Binding MonitoringStatus}"
                                   Style="{StaticResource SmallMutedTextStyle}"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2"
                    Style="{StaticResource PagePanelStyle}"
                    Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="42"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="52"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0"
                            BorderBrush="{StaticResource BorderBrushSoft}"
                            BorderThickness="0 0 0 1"
                            Padding="14 0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                <Ellipse Width="7"
                                         Height="7"
                                         Fill="{StaticResource SuccessBrush}"
                                         Margin="0 0 10 0"/>
                                <TextBlock Text="Terminal"
                                           Style="{StaticResource SectionTitleTextStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        VerticalAlignment="Center">
                                <TextBlock Text="SSH"
                                           FontWeight="SemiBold"
                                           Margin="0 0 12 0"/>
                                <Button Style="{StaticResource CompactButtonStyle}"
                                        MinWidth="110"
                                        ToolTip="{Binding [RefreshMonitoring], Source={x:Static services:AppServices.Localizer}}"
                                        Command="{Binding RefreshMonitoringCommand}"
                                        Margin="0 0 8 0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Refresh"
                                                                    Style="{StaticResource PackIconStyle}"
                                                                    Margin="0 0 8 0"/>
                                        <TextBlock Text="{Binding [Refresh], Source={x:Static services:AppServices.Localizer}}"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource CompactButtonStyle}"
                                        MinWidth="100"
                                        ToolTip="{Binding [ClearTerminal], Source={x:Static services:AppServices.Localizer}}"
                                        Command="{Binding ClearTerminalCommand}"
                                        Margin="0 0 8 0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Broom"
                                                                    Style="{StaticResource PackIconStyle}"
                                                                    Margin="0 0 8 0"/>
                                        <TextBlock Text="{Binding [Clear], Source={x:Static services:AppServices.Localizer}}"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource IconButtonStyle}"
                                        ToolTip="{Binding [More], Source={x:Static services:AppServices.Localizer}}"
                                        Click="OpenButtonContextMenu_Click">
                                    <Button.Content>
                                        <iconPacks:PackIconMaterial Kind="DotsHorizontal"
                                                                    Style="{StaticResource PackIconStyle}"/>
                                    </Button.Content>
                                    <Button.ContextMenu>
                                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                            <MenuItem Header="{Binding [AddServer], Source={x:Static services:AppServices.Localizer}}"
                                                      Command="{Binding AddServerCommand}">
                                                <MenuItem.Icon>
                                                    <iconPacks:PackIconMaterial Kind="Plus"
                                                                                Style="{StaticResource PackIconStyle}"/>
                                                </MenuItem.Icon>
                                            </MenuItem>
                                            <MenuItem Header="{Binding [Files], Source={x:Static services:AppServices.Localizer}}"
                                                      Command="{Binding LoadRemoteFilesCommand}">
                                                <MenuItem.Icon>
                                                    <iconPacks:PackIconMaterial Kind="Folder"
                                                                                Style="{StaticResource PackIconStyle}"/>
                                                </MenuItem.Icon>
                                            </MenuItem>
                                            <Separator/>
                                            <MenuItem Header="{Binding [Settings], Source={x:Static services:AppServices.Localizer}}"
                                                      Command="{Binding OpenSettingsPanelCommand}">
                                                <MenuItem.Icon>
                                                    <iconPacks:PackIconMaterial Kind="Cog"
                                                                                Style="{StaticResource PackIconStyle}"/>
                                                </MenuItem.Icon>
                                            </MenuItem>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <TextBox Grid.Row="1"
                             Text="{Binding TerminalOutput}"
                             IsReadOnly="True"
                             Style="{StaticResource ConsoleTextBoxStyle}"
                             BorderThickness="0"/>
                    <Grid Grid.Row="2"
                          Margin="12 6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding TerminalCommand, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="SSH command"
                                 VerticalContentAlignment="Center"
                                 KeyDown="TerminalCommandTextBox_KeyDown"/>
                        <Button Grid.Column="2"
                                Style="{StaticResource PrimaryButtonStyle}"
                                ToolTip="Send SSH command"
                                Command="{Binding SendTerminalCommandCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding [Send], Source={x:Static services:AppServices.Localizer}}"
                                           FontWeight="SemiBold"
                                           Margin="0 0 8 0"/>
                                <iconPacks:PackIconMaterial Kind="Send"
                                                            Style="{StaticResource PackIconStyle}"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Grid>
            </Border>

            <GridSplitter Grid.Row="3"
                          Height="8"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Center"
                          Background="Transparent"
                          ShowsPreview="True"
                          ResizeDirection="Rows">
                <GridSplitter.Template>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Border Background="Transparent"
                                Padding="0 3">
                            <Border Height="2"
                                    CornerRadius="1"
                                    Background="{StaticResource BorderBrushStrong}"
                                    Opacity="0.8"/>
                        </Border>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>
            <Border Grid.Row="4"
                    Style="{StaticResource PagePanelStyle}"
                    Padding="0">

                <TabControl Style="{StaticResource ModernTabControlStyle}"
                            ItemContainerStyle="{StaticResource ModernTabItemStyle}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="0">

                    <TabItem>
                        <TabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Folder"
                                                            Style="{StaticResource PackIconStyle}"
                                                            Margin="0 0 8 0"/>
                                <TextBlock Text="{Binding [Files], Source={x:Static services:AppServices.Localizer}}"/>
                            </StackPanel>
                        </TabItem.Header>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="56"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="32"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="0 0 0 1"
                                    Padding="12 8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Style="{StaticResource CompactButtonStyle}"
                                            ToolTip="{Binding [LoadRoot], Source={x:Static services:AppServices.Localizer}}"
                                            Command="{Binding GoRootRemoteFolderCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Home"
                                                                        Style="{StaticResource PackIconStyle}"
                                                                        Margin="0 0 8 0"/>
                                            <TextBlock Text="{Binding [Home], Source={x:Static services:AppServices.Localizer}}"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Grid.Column="2"
                                            Style="{StaticResource CompactButtonStyle}"
                                            ToolTip="{Binding [ParentFolder], Source={x:Static services:AppServices.Localizer}}"
                                            Command="{Binding GoBackRemoteFolderCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="ArrowLeft"
                                                                        Style="{StaticResource PackIconStyle}"
                                                                        Margin="0 0 8 0"/>
                                            <TextBlock Text="{Binding [Back], Source={x:Static services:AppServices.Localizer}}"/>
                                        </StackPanel>
                                    </Button>
                                    <Border Grid.Column="4"
                                            Background="#08131D"
                                            BorderBrush="{StaticResource BorderBrushStrong}"
                                            BorderThickness="1"
                                            CornerRadius="7"
                                            Padding="10 0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <iconPacks:PackIconMaterial Grid.Column="0"
                                                                        Kind="Folder"
                                                                        Style="{StaticResource PackIconStyle}"
                                                                        Foreground="{StaticResource AccentBrush}"/>
                                            <TextBox Grid.Column="2"
                                                     Text="{Binding RemoteFolderPath, UpdateSourceTrigger=PropertyChanged}"
                                                     BorderThickness="0"
                                                     Background="Transparent"
                                                     Padding="0"
                                                     MinHeight="30"
                                                     VerticalContentAlignment="Center"
                                                     ToolTip="{Binding [DefaultRemoteFolder], Source={x:Static services:AppServices.Localizer}}"/>
                                        </Grid>
                                    </Border>
                                    <StackPanel Grid.Column="6"
                                                Orientation="Horizontal">
                                        <Button Style="{StaticResource CompactButtonStyle}"
                                                ToolTip="{Binding [CreateFolder], Source={x:Static services:AppServices.Localizer}}"
                                                Click="RemoteFileCreateFolderMenuItem_Click"
                                                Margin="0 0 6 0">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial Kind="FolderPlus"
                                                                            Style="{StaticResource PackIconStyle}"
                                                                            Margin="0 0 8 0"/>
                                                <TextBlock Text="{Binding [Folder], Source={x:Static services:AppServices.Localizer}}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource CompactButtonStyle}"
                                                ToolTip="{Binding [Upload], Source={x:Static services:AppServices.Localizer}}"
                                                Command="{Binding UploadFileToCurrentFolderCommand}"
                                                Margin="0 0 6 0">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial Kind="Upload"
                                                                            Style="{StaticResource PackIconStyle}"
                                                                            Margin="0 0 8 0"/>
                                                <TextBlock Text="{Binding [Upload], Source={x:Static services:AppServices.Localizer}}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource CompactButtonStyle}"
                                                ToolTip="{Binding [Download], Source={x:Static services:AppServices.Localizer}}"
                                                Command="{Binding DownloadSelectedRemoteFileCommand}"
                                                Margin="0 0 6 0">
                                            <StackPanel Orientation="Horizontal">
                                                <iconPacks:PackIconMaterial Kind="Download"
                                                                            Style="{StaticResource PackIconStyle}"
                                                                            Margin="0 0 8 0"/>
                                                <TextBlock Text="{Binding [Download], Source={x:Static services:AppServices.Localizer}}"/>
                                            </StackPanel>
                                        </Button>
                                        <Button Style="{StaticResource IconButtonStyle}"
                                                ToolTip="{Binding [Refresh], Source={x:Static services:AppServices.Localizer}}"
                                                Command="{Binding LoadRemoteFilesCommand}"
                                                Margin="0 0 6 0">
                                            <iconPacks:PackIconMaterial Kind="Refresh"
                                                                        Style="{StaticResource PackIconStyle}"/>
                                        </Button>
                                        <Button Style="{StaticResource IconButtonStyle}"
                                                ToolTip="{Binding [More], Source={x:Static services:AppServices.Localizer}}"
                                                Click="OpenButtonContextMenu_Click">
                                            <Button.Content>
                                                <iconPacks:PackIconMaterial Kind="DotsHorizontal"
                                                                            Style="{StaticResource PackIconStyle}"/>
                                            </Button.Content>
                                            <Button.ContextMenu>
                                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                                    <MenuItem Header="{Binding [Open], Source={x:Static services:AppServices.Localizer}}"
                                                              Click="RemoteFileOpenMenuItem_Click">
                                                        <MenuItem.Icon>
                                                            <iconPacks:PackIconMaterial Kind="OpenInNew"
                                                                                        Style="{StaticResource PackIconStyle}"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="{Binding [Edit], Source={x:Static services:AppServices.Localizer}}"
                                                              Click="RemoteFileEditMenuItem_Click">
                                                        <MenuItem.Icon>
                                                            <iconPacks:PackIconMaterial Kind="Pencil"
                                                                                        Style="{StaticResource PackIconStyle}"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="{Binding [Download], Source={x:Static services:AppServices.Localizer}}"
                                                              Click="RemoteFileDownloadMenuItem_Click">
                                                        <MenuItem.Icon>
                                                            <iconPacks:PackIconMaterial Kind="Download"
                                                                                        Style="{StaticResource PackIconStyle}"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <Separator/>
                                                    <MenuItem Header="{Binding [Rename], Source={x:Static services:AppServices.Localizer}}"
                                                              Click="RemoteFileRenameMenuItem_Click">
                                                        <MenuItem.Icon>
                                                            <iconPacks:PackIconMaterial Kind="RenameBox"
                                                                                        Style="{StaticResource PackIconStyle}"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                    <MenuItem Header="{Binding [Delete], Source={x:Static services:AppServices.Localizer}}"
                                                              Click="RemoteFileDeleteMenuItem_Click">
                                                        <MenuItem.Icon>
                                                            <iconPacks:PackIconMaterial Kind="Delete"
                                                                                        Style="{StaticResource PackIconStyle}"
                                                                                        Foreground="{StaticResource DangerBrush}"/>
                                                        </MenuItem.Icon>
                                                    </MenuItem>
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <DataGrid Grid.Row="1"
                                      ItemsSource="{Binding RemoteFiles}"
                                      SelectedItem="{Binding SelectedRemoteFile}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      CanUserAddRows="False"
                                      AllowDrop="True"
                                      RowHeight="46"
                                      PreviewMouseRightButtonDown="RemoteFilesDataGrid_PreviewMouseRightButtonDown"
                                      DragOver="RemoteFilesListView_DragOver"
                                      Drop="RemoteFilesListView_Drop"
                                      MouseDoubleClick="RemoteFilesListView_MouseDoubleClick">
                                <DataGrid.ContextMenu>
                                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                        <MenuItem Header="{Binding [Open], Source={x:Static services:AppServices.Localizer}}"
                                                  Click="RemoteFileOpenMenuItem_Click">
                                            <MenuItem.Icon>
                                                <iconPacks:PackIconMaterial Kind="OpenInNew"
                                                                            Style="{StaticResource PackIconStyle}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="{Binding [Edit], Source={x:Static services:AppServices.Localizer}}"
                                                  Click="RemoteFileEditMenuItem_Click">
                                            <MenuItem.Icon>
                                                <iconPacks:PackIconMaterial Kind="Pencil"
                                                                            Style="{StaticResource PackIconStyle}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="{Binding [Download], Source={x:Static services:AppServices.Localizer}}"
                                                  Click="RemoteFileDownloadMenuItem_Click">
                                            <MenuItem.Icon>
                                                <iconPacks:PackIconMaterial Kind="Download"
                                                                            Style="{StaticResource PackIconStyle}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="{Binding [CreateFolder], Source={x:Static services:AppServices.Localizer}}"
                                                  Click="RemoteFileCreateFolderMenuItem_Click">
                                            <MenuItem.Icon>
                                                <iconPacks:PackIconMaterial Kind="FolderPlus"
                                                                            Style="{StaticResource PackIconStyle}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="{Binding [Rename], Source={x:Static services:AppServices.Localizer}}"
                                                  Click="RemoteFileRenameMenuItem_Click">
                                            <MenuItem.Icon>
                                                <iconPacks:PackIconMaterial Kind="RenameBox"
                                                                            Style="{StaticResource PackIconStyle}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <Separator/>
                                        <MenuItem Header="{Binding [Delete], Source={x:Static services:AppServices.Localizer}}"
                                                  Click="RemoteFileDeleteMenuItem_Click">
                                            <MenuItem.Icon>
                                                <iconPacks:PackIconMaterial Kind="Delete"
                                                                            Style="{StaticResource PackIconStyle}"
                                                                            Foreground="{StaticResource DangerBrush}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="{Binding [Name], Source={x:Static services:AppServices.Localizer}}"
                                                            Width="*"
                                                            MinWidth="280">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="38"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border x:Name="FileBadge"
                                                            Width="30"
                                                            Height="30"
                                                            CornerRadius="8"
                                                            Background="#162434"
                                                            HorizontalAlignment="Left">
                                                        <iconPacks:PackIconMaterial x:Name="FileIcon"
                                                                                    Kind="FileDocumentOutline"
                                                                                    Style="{StaticResource PackIconStyle}"
                                                                                    Width="17"
                                                                                    Height="17"
                                                                                    Foreground="{StaticResource MutedTextBrush}"/>
                                                    </Border>
                                                    <StackPanel Grid.Column="1"
                                                                VerticalAlignment="Center">
                                                        <TextBlock x:Name="FileName"
                                                                   Text="{Binding Name}"
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding FullPath}"
                                                                   Style="{StaticResource TinyMutedTextStyle}"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   Margin="0 2 0 0"/>
                                                    </StackPanel>
                                                </Grid>
                                                <DataTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding IsDirectory}"
                                                                 Value="True">
                                                        <Setter TargetName="FileBadge"
                                                                Property="Background"
                                                                Value="{StaticResource AccentSoftBrush}"/>
                                                        <Setter TargetName="FileIcon"
                                                                Property="Kind"
                                                                Value="Folder"/>
                                                        <Setter TargetName="FileIcon"
                                                                Property="Foreground"
                                                                Value="{StaticResource AccentBrush}"/>
                                                        <Setter TargetName="FileName"
                                                                Property="Foreground"
                                                                Value="{StaticResource TextBrush}"/>
                                                    </DataTrigger>
                                                </DataTemplate.Triggers>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="{Binding [Type], Source={x:Static services:AppServices.Localizer}}"
                                                        Binding="{Binding TypeText}"
                                                        Width="110"/>
                                    <DataGridTextColumn Header="{Binding [Size], Source={x:Static services:AppServices.Localizer}}"
                                                        Binding="{Binding SizeText}"
                                                        Width="110"/>
                                    <DataGridTextColumn Header="{Binding [Modified], Source={x:Static services:AppServices.Localizer}}"
                                                        Binding="{Binding LastWriteTimeText}"
                                                        Width="150"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Border Grid.Row="2"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="0 1 0 0"
                                    Padding="12 6">
                                <TextBlock Text="{Binding SftpOutput}"
                                           Style="{StaticResource TinyMutedTextStyle}"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </TabItem>
                    <TabItem Header="{Binding [Logs], Source={x:Static services:AppServices.Localizer}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="46"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="0 0 0 1"
                                    Padding="10 6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox Text="{Binding LogPath, UpdateSourceTrigger=PropertyChanged}"
                                             ToolTip="{Binding [DefaultLogPath], Source={x:Static services:AppServices.Localizer}}"/>

                                    <StackPanel Grid.Column="2"
                                                Orientation="Horizontal">
                                        <Button Content="nginx"
                                                Style="{StaticResource CompactButtonStyle}"
                                                ToolTip="Open nginx log"
                                                Command="{Binding UseNginxLogCommand}"
                                                Margin="0 0 6 0"/>

                                        <Button Content="syslog"
                                                Style="{StaticResource CompactButtonStyle}"
                                                ToolTip="Open syslog"
                                                Command="{Binding UseSyslogCommand}"
                                                Margin="0 0 6 0"/>

                                        <Button Style="{StaticResource IconButtonStyle}"
                                                ToolTip="{Binding [RefreshLogs], Source={x:Static services:AppServices.Localizer}}"
                                                Command="{Binding LoadLogCommand}">
                                            <iconPacks:PackIconMaterial Kind="Refresh"
                                                                        Style="{StaticResource PackIconStyle}"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <TextBox Grid.Row="1"
                                     Text="{Binding LogContent}"
                                     IsReadOnly="True"
                                     Style="{StaticResource ConsoleTextBoxStyle}"
                                     BorderThickness="0"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="{Binding [Commands], Source={x:Static services:AppServices.Localizer}}">
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.25*"
                                                  MinWidth="420"/>
                                <ColumnDefinition Width="14"/>
                                <ColumnDefinition Width="*"
                                                  MinWidth="340"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0"
                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                    BorderThickness="1"
                                    CornerRadius="7"
                                    Background="#08131D">
                                <ListBox ItemsSource="{Binding SavedCommands}"
                                         SelectedItem="{Binding SelectedSavedCommand}"
                                         Margin="8"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="12 10"
                                                    CornerRadius="7"
                                                    BorderBrush="{StaticResource BorderBrushSoft}"
                                                    BorderThickness="1"
                                                    Background="{StaticResource TableRowBrush}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Title}"
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding Command}"
                                                                   Style="{StaticResource SmallMutedTextStyle}"
                                                                   FontFamily="Consolas"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   Margin="0 4 0 0"/>
                                                    </StackPanel>
                                                    <iconPacks:PackIconMaterial Grid.Column="1"
                                                                                Kind="ClockOutline"
                                                                                Style="{StaticResource PackIconStyle}"
                                                                                Foreground="{StaticResource MutedTextBrush}"
                                                                                Margin="12 0 0 0"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>
                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="12"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBox Text="{Binding CommandTitle, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Название команды"/>
                                <TextBox Grid.Row="2"
                                         Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         FontFamily="Consolas"
                                         ToolTip="Команда"/>
                                <Grid Grid.Row="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            HorizontalAlignment="Left"
                                            Command="{Binding AddSavedCommandCommand}"
                                            ToolTip="{Binding [AddCommand], Source={x:Static services:AppServices.Localizer}}">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Plus"
                                                                        Style="{StaticResource PackIconStyle}"
                                                                        Margin="0 0 8 0"/>
                                            <TextBlock Text="{Binding [AddCommand], Source={x:Static services:AppServices.Localizer}}"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Grid.Column="1"
                                            Style="{StaticResource IconButtonStyle}"
                                            ToolTip="Сохранить команду"
                                            Command="{Binding UpdateSavedCommandCommand}"
                                            Margin="0 0 6 0">
                                        <iconPacks:PackIconMaterial Kind="ContentSave"
                                                                    Style="{StaticResource PackIconStyle}"/>
                                    </Button>
                                    <Button Grid.Column="2"
                                            Style="{StaticResource IconButtonStyle}"
                                            ToolTip="{Binding [ToggleFavorite], Source={x:Static services:AppServices.Localizer}}"
                                            Command="{Binding ToggleSavedCommandFavoriteCommand}"
                                            Margin="0 0 6 0">
                                        <iconPacks:PackIconMaterial Kind="Star"
                                                                    Style="{StaticResource PackIconStyle}"/>
                                    </Button>
                                    <Button Grid.Column="3"
                                            Style="{StaticResource IconButtonStyle}"
                                            ToolTip="{Binding [Delete], Source={x:Static services:AppServices.Localizer}}"
                                            Command="{Binding DeleteSavedCommandCommand}">
                                        <iconPacks:PackIconMaterial Kind="Delete"
                                                                    Style="{StaticResource PackIconStyle}"
                                                                    Foreground="{StaticResource DangerBrush}"/>
                                    </Button>
                                    <Button Grid.Column="5"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            ToolTip="{Binding [Run], Source={x:Static services:AppServices.Localizer}}"
                                            Command="{Binding RunSavedCommandCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <iconPacks:PackIconMaterial Kind="Play"
                                                                        Style="{StaticResource PackIconStyle}"
                                                                        Foreground="White"
                                                                        Margin="0 0 8 0"/>
                                            <TextBlock Text="{Binding [Run], Source={x:Static services:AppServices.Localizer}}"
                                                       FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Grid>
                    </TabItem>
                    <TabItem Header="{Binding [ServerInspector], Source={x:Static services:AppServices.Localizer}}">
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.1*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="1.4*"/>
                            </Grid.ColumnDefinitions>
                            <ScrollViewer Grid.Column="0"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled">
                                <StackPanel>
                                    <Border Style="{StaticResource SoftCardBorderStyle}"
                                            Margin="0 0 0 10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding SelectedServerTitle}"
                                                       Style="{StaticResource TitleTextStyle}"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding SelectedServerSubtitle}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       Margin="0 5 0 0"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding SelectedServerOs}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       Margin="0 3 0 0"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{StaticResource SoftCardBorderStyle}">
                                        <StackPanel>
                                            <TextBlock Text="Статус"
                                                       Style="{StaticResource SectionTitleTextStyle}"
                                                       Margin="0 0 0 8"/>
                                            <TextBlock Text="{Binding MonitorLoad}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding MonitorProcesses}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       Margin="0 8 0 0"
                                                       TextWrapping="Wrap"
                                                       FontFamily="Consolas"/>
                                            <TextBlock Text="{Binding MonitorNetwork}"
                                                       Style="{StaticResource SmallMutedTextStyle}"
                                                       Margin="0 8 0 0"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                            <Border Grid.Column="2"
                                    Style="{StaticResource SoftCardBorderStyle}"
                                    Padding="0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="44"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0"
                                            BorderBrush="{StaticResource BorderBrushSoft}"
                                            BorderThickness="0 0 0 1"
                                            Padding="12 0">
                                        <Grid>
                                            <TextBlock Text="{Binding [RecentLogs], Source={x:Static services:AppServices.Localizer}}"
                                                       Style="{StaticResource SectionTitleTextStyle}"/>
                                            <Button Content="{Binding [ViewAll], Source={x:Static services:AppServices.Localizer}}"
                                                    BorderThickness="0"
                                                    Background="Transparent"
                                                    Foreground="{StaticResource AccentBrush}"
                                                    HorizontalAlignment="Right"
                                                    ToolTip="{Binding [ActivityLog], Source={x:Static services:AppServices.Localizer}}"
                                                    Command="{Binding OpenActivityPanelCommand}"
                                                    Padding="4 0"/>
                                        </Grid>
                                    </Border>
                                    <ListBox Grid.Row="1"
                                             ItemsSource="{Binding RecentLogs}"
                                             Margin="8"
                                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Border Padding="9 7"
                                                        CornerRadius="6"
                                                        Background="{StaticResource TableRowBrush}"
                                                        BorderBrush="{StaticResource BorderBrushSoft}"
                                                        BorderThickness="1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="58"/>
                                                            <ColumnDefinition Width="72"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <Ellipse Grid.Column="0"
                                                                 Width="8"
                                                                 Height="8"
                                                                 Margin="0 5 10 0">
                                                            <Ellipse.Style>
                                                                <Style TargetType="Ellipse">
                                                                    <Setter Property="Fill"
                                                                            Value="{StaticResource SuccessBrush}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Level}"
                                                                                     Value="warn">
                                                                            <Setter Property="Fill"
                                                                                    Value="{StaticResource WarningBrush}"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Level}"
                                                                                     Value="error">
                                                                            <Setter Property="Fill"
                                                                                    Value="{StaticResource DangerBrush}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Ellipse.Style>
                                                        </Ellipse>
                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Time}"
                                                                   Style="{StaticResource SmallMutedTextStyle}"/>
                                                        <TextBlock Grid.Column="2"
                                                                   Text="{Binding Service}"
                                                                   Style="{StaticResource SmallMutedTextStyle}"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Grid.Column="3"
                                                                   Text="{Binding Message}"
                                                                   Style="{StaticResource SmallMutedTextStyle}"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                            </Border>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>

        </Grid>

        <Border Grid.ColumnSpan="2"
                Grid.RowSpan="2"
                Background="#99050A0F"
                Visibility="{Binding IsProfilePanelOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Style="{StaticResource PagePanelStyle}"
                    Width="520"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0 0 0 14">
                        <TextBlock Text="{Binding [Profile], Source={x:Static services:AppServices.Localizer}}"
                                   Style="{StaticResource TitleTextStyle}"/>
                        <Button Style="{StaticResource IconButtonStyle}"
                                HorizontalAlignment="Right"
                                ToolTip="{Binding [Close], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding CloseOverlayPanelsCommand}">
                            <iconPacks:PackIconMaterial Kind="Close"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                    </Grid>
                    <Border Style="{StaticResource SoftCardBorderStyle}"
                            Margin="0 0 0 14">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Width="52"
                                    Height="52"
                                    CornerRadius="26"
                                    Background="{StaticResource AccentBrush}"
                                    Margin="0 0 14 0">
                                <TextBlock Text="{Binding UserProfile.Initials}"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1"
                                        VerticalAlignment="Center">
                                <TextBlock Text="{Binding UserProfile.DisplayName}"
                                           Style="{StaticResource SectionTitleTextStyle}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding UserProfile.Role}"
                                           Style="{StaticResource SmallMutedTextStyle}"
                                           Margin="0 3 0 0"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding UserProfile.Email}"
                                           Style="{StaticResource SmallMutedTextStyle}"
                                           Margin="0 2 0 0"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <TextBlock Text="{Binding [Name], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding UserProfile.DisplayName, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 12"/>
                    <TextBlock Text="{Binding [Initials], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding UserProfile.Initials, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 12"/>
                    <TextBlock Text="{Binding [Role], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding UserProfile.Role, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 12"/>
                    <TextBlock Text="Email"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding UserProfile.Email, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 16"/>
                    <Button Content="{Binding [SaveProfile], Source={x:Static services:AppServices.Localizer}}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            ToolTip="{Binding [SaveProfile], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding SaveProfileCommand}"/>
                </StackPanel>
            </Border>
        </Border>

        <Border Grid.ColumnSpan="2"
                Grid.RowSpan="2"
                Background="#99050A0F"
                Visibility="{Binding IsSettingsPanelOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Style="{StaticResource PagePanelStyle}"
                    Width="560"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0 0 0 14">
                        <TextBlock Text="{Binding [Settings], Source={x:Static services:AppServices.Localizer}}"
                                   Style="{StaticResource TitleTextStyle}"/>
                        <Button Style="{StaticResource IconButtonStyle}"
                                HorizontalAlignment="Right"
                                ToolTip="{Binding [Close], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding CloseOverlayPanelsCommand}">
                            <iconPacks:PackIconMaterial Kind="Close"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                    </Grid>
                    <TextBlock Text="{Binding [Language], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <ComboBox ItemsSource="{Binding LanguageOptions}"
                              SelectedValue="{Binding AppSettings.LanguageCode, UpdateSourceTrigger=PropertyChanged}"
                              SelectedValuePath="Code"
                              DisplayMemberPath="Name"
                              Margin="0 6 0 12"/>
                    <TextBlock Text="{Binding [MonitoringInterval], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding AppSettings.MonitoringIntervalSeconds, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 12"/>
                    <TextBlock Text="{Binding [DefaultRemoteFolder], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding AppSettings.DefaultRemoteFolder, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 12"/>
                    <TextBlock Text="{Binding [DefaultLogPath], Source={x:Static services:AppServices.Localizer}}"
                               Style="{StaticResource SmallMutedTextStyle}"/>
                    <TextBox Text="{Binding AppSettings.DefaultLogPath, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0 6 0 12"/>
                    <CheckBox Content="{Binding [AutoRefreshMonitoring], Source={x:Static services:AppServices.Localizer}}"
                              IsChecked="{Binding AppSettings.AutoRefreshMonitoring}"
                              Margin="0 0 0 16"/>
                    <Button Content="{Binding [SaveSettings], Source={x:Static services:AppServices.Localizer}}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            ToolTip="{Binding [SaveSettings], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding SaveSettingsCommand}"/>
                </StackPanel>
            </Border>
        </Border>

        <Border Grid.ColumnSpan="2"
                Grid.RowSpan="2"
                Background="#99050A0F"
                Visibility="{Binding IsFavoritesPanelOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Style="{StaticResource PagePanelStyle}"
                    Width="680"
                    Height="520"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0 0 0 14">
                        <TextBlock Text="{Binding [Favorites], Source={x:Static services:AppServices.Localizer}}"
                                   Style="{StaticResource TitleTextStyle}"/>
                        <Button Style="{StaticResource IconButtonStyle}"
                                HorizontalAlignment="Right"
                                ToolTip="{Binding [Close], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding CloseOverlayPanelsCommand}">
                            <iconPacks:PackIconMaterial Kind="Close"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding [Servers], Source={x:Static services:AppServices.Localizer}}"
                                       Style="{StaticResource SectionTitleTextStyle}"
                                       Margin="0 0 0 10"/>
                            <ListBox ItemsSource="{Binding FavoriteServers}"
                                     SelectedItem="{Binding SelectedServer}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource SoftCardBorderStyle}"
                                                Margin="0 0 0 8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}"
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Host}"
                                                           Style="{StaticResource SmallMutedTextStyle}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="{Binding [Commands], Source={x:Static services:AppServices.Localizer}}"
                                       Style="{StaticResource SectionTitleTextStyle}"
                                       Margin="0 0 0 10"/>
                            <ListBox ItemsSource="{Binding FavoriteCommands}"
                                     SelectedItem="{Binding SelectedSavedCommand}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource SoftCardBorderStyle}"
                                                Margin="0 0 0 8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}"
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Command}"
                                                           Style="{StaticResource SmallMutedTextStyle}"
                                                           FontFamily="Consolas"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Border>

        <Border Grid.ColumnSpan="2"
                Grid.RowSpan="2"
                Background="#99050A0F"
                Visibility="{Binding IsActivityPanelOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Style="{StaticResource PagePanelStyle}"
                    Width="760"
                    Height="560"
                    Padding="18"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0 0 0 14">
                        <TextBlock Text="{Binding [ActivityLog], Source={x:Static services:AppServices.Localizer}}"
                                   Style="{StaticResource TitleTextStyle}"/>
                        <Button Style="{StaticResource IconButtonStyle}"
                                HorizontalAlignment="Right"
                                ToolTip="{Binding [Close], Source={x:Static services:AppServices.Localizer}}"
                                Command="{Binding CloseOverlayPanelsCommand}">
                            <iconPacks:PackIconMaterial Kind="Close"
                                                        Style="{StaticResource PackIconStyle}"/>
                        </Button>
                    </Grid>
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding ActivityLogs}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              RowHeight="36">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{Binding [Time], Source={x:Static services:AppServices.Localizer}}"
                                                Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM HH:mm:ss}}"
                                                Width="130"/>
                            <DataGridTextColumn Header="{Binding [Activity], Source={x:Static services:AppServices.Localizer}}"
                                                Binding="{Binding Action}"
                                                Width="120"/>
                            <DataGridTextColumn Header="{Binding [Target], Source={x:Static services:AppServices.Localizer}}"
                                                Binding="{Binding Target}"
                                                Width="150"/>
                            <DataGridTextColumn Header="{Binding [Details], Source={x:Static services:AppServices.Localizer}}"
                                                Binding="{Binding Details}"
                                                Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Grid.Row="2"
                            Content="{Binding [ClearLog], Source={x:Static services:AppServices.Localizer}}"
                            Style="{StaticResource DangerButtonStyle}"
                            HorizontalAlignment="Right"
                            Margin="0 12 0 0"
                            ToolTip="{Binding [ClearLog], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding ClearActivityLogsCommand}"/>
                </Grid>
            </Border>
        </Border>
    </Grid>

</Window>
