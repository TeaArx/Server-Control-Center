<Window x:Class="ServerControlCenter.Views.ServerEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Height="520"
        Width="620"
        MinWidth="560"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource AppBackgroundBrush}">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 14">
            <TextBlock Text="{Binding WindowTitle}" Style="{StaticResource TitleTextStyle}"/>
            <TextBlock Text="Профиль подключения хранится локально в SQLite." Style="{StaticResource SmallMutedTextStyle}" Margin="0 4 0 0"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource PagePanelStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Название" Style="{StaticResource SmallMutedTextStyle}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="IP / Host" Style="{StaticResource SmallMutedTextStyle}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Порт" Style="{StaticResource SmallMutedTextStyle}"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Логин" Style="{StaticResource SmallMutedTextStyle}"/>
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="8" Grid.Column="0" Text="Пароль" Style="{StaticResource SmallMutedTextStyle}"/>
                <PasswordBox x:Name="PasswordInput"
                             Grid.Row="8"
                             Grid.Column="1"
                             PasswordChanged="PasswordInput_PasswordChanged"/>

                <TextBlock Grid.Row="10" Grid.Column="0" Text="SSH-ключ" Style="{StaticResource SmallMutedTextStyle}"/>
                <Grid Grid.Row="10" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding PrivateKeyPath, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Grid.Column="2" Content="Выбрать" MinWidth="92" Command="{Binding PickPrivateKeyCommand}"/>
                </Grid>

                <TextBlock Grid.Row="12" Grid.Column="0" Text="Заметки" Style="{StaticResource SmallMutedTextStyle}" VerticalAlignment="Top" Margin="0 8 0 0"/>
                <TextBox Grid.Row="12" Grid.Column="1"
                         Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0 14 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Style="{StaticResource SmallMutedTextStyle}" TextWrapping="Wrap"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Проверить SSH" MinWidth="124" Margin="0 0 8 0" Command="{Binding TestConnectionCommand}"/>
                <Button Content="Отмена" MinWidth="92" Margin="0 0 8 0" IsCancel="True"/>
                <Button Content="Сохранить" MinWidth="110" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
