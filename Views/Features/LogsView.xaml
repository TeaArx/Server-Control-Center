<UserControl x:Class="ServerControlCenter.Views.Features.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:ServerControlCenter.Controls"
             xmlns:services="clr-namespace:ServerControlCenter.Services"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="46"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="0 0 0 1"
                Padding="10 6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="LogPathTextBox"
                         Text="{Binding LogPath, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="LogPathTextBox_KeyDown"
                         ToolTip="{Binding [DefaultLogPath], Source={x:Static services:AppServices.Localizer}}"/>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal">
                    <Button Content="nginx"
                            Style="{StaticResource CompactButtonStyle}"
                            ToolTip="{Binding [OpenNginxLog], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding UseNginxLogCommand}"
                            Margin="0 0 6 0"/>

                    <Button Content="syslog"
                            Style="{StaticResource CompactButtonStyle}"
                            ToolTip="{Binding [OpenSyslog], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding UseSyslogCommand}"
                            Margin="0 0 6 0"/>

                    <Button Style="{StaticResource IconButtonStyle}"
                            ToolTip="{Binding [RefreshLogs], Source={x:Static services:AppServices.Localizer}}"
                            Command="{Binding LoadLogCommand}">
                        <iconPacks:PackIconMaterial Kind="Refresh"
                                                    Style="{StaticResource PackIconStyle}"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <ProgressBar Grid.Row="1"
                     Height="3"
                     VerticalAlignment="Top"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLogLoading, Converter={StaticResource BoolToVisibility}}"
                     Panel.ZIndex="2"/>
        <TextBox Grid.Row="1"
                 Text="{Binding LogContent}"
                 IsReadOnly="True"
                 Style="{StaticResource ConsoleTextBoxStyle}"
                 BorderThickness="0"/>
    </Grid>
</UserControl>
